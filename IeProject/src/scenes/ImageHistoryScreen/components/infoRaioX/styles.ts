import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonComponent: {
     marginStart: 30,
     marginTop: 10,
  },

  Button: {
    borderRadius: 5,
    borderWidth: 2,
    justifyContent: 'center',
      backgroundColor: "#688591",
      width: 350,
      height: 50,
      borderColor: "#688591"
  },

  buttonComponentText: {
    marginStart: 5,
    fontSize: 15,
    color: '#FFF',
    fontWeight: 'normal',
  },
});

export default styles;
